{
  "name": "nexus-bot",
  "version": "1.0.0",
  "description": "Check appointment times for Nexus",
  "scripts": {
    "build": "rm -rf ./dist && tsc",
    "lint": "tsc --noEmit",
    "example": "ts-node example.ts",
    "format": "prettier --write --no-error-on-unmatched-pattern {**/*,*}.{js,ts,jsx,tsx}"
  },
  "author": "John Smart",
  "license": "GPLV3",
  "dependencies": {
    "node-fetch": "2"
  },
  "devDependencies": {
    "@types/node": "^18.0.3",
    "@types/node-fetch": "^2.6.2",
    "prettier": "^2.7.1",
    "ts-node": "^10.8.2",
    "tslib": "^2.4.0",
    "typescript": "^4.7.4"
  }
}
